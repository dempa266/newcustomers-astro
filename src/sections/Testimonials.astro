---
import { parseEncodedJson } from '../components/utils';

interface Testimonial {
	text?: string;
	image?: { url?: string; alt?: string };
	logo?: { url?: string; alt?: string };
}

interface Props {
	data?: {
		testimonials?: string;
	};
}

const { data = {} } = Astro.props as Props;
const testimonials = parseEncodedJson<Testimonial[]>(data.testimonials, []);
---

<section class="section-spacing">
	<div class="site-container">
		<div class="grid gap-8 lg:grid-cols-2 text-dark">
			{testimonials.map((testimonial) => (
				<div class="flex flex-col space-y-12 bg-gray-light rounded-xl overflow-hidden">
					{testimonial.image?.url && (
						<img
							class="w-full"
							src={testimonial.image.url}
							alt={testimonial.image.alt ?? ''}
							width="800"
							height="400"
							loading="lazy"
						/>
					)}
					<p class="leading-relaxed px-6 lg:px-12 text-lg text-center text-gray-800">{testimonial.text}</p>
					{testimonial.logo?.url && (
						<img
							class="w-48 h-auto mx-auto pb-12"
							src={testimonial.logo.url}
							alt={testimonial.logo.alt ?? ''}
							width="200"
							height="200"
							loading="lazy"
						/>
					)}
				</div>
			))}
		</div>
	</div>
</section>
